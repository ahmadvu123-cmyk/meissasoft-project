import { UpdateWorkerResponseDto } from './update.worker.response.dto';

describe('UpdateWorkerResponseDto', () => {
  it('should be defined', () => {
    expect(new UpdateWorkerResponseDto()).toBeDefined();
  });
});
