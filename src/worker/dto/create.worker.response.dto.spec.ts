import { CreateWorkerResponseDto } from '../create.worker.response.dto';

describe('CreateWorkerResponseDto', () => {
  it('should be defined', () => {
    expect(new CreateWorkerResponseDto()).toBeDefined();
  });
});
