import { UpdateWageResponseDto } from './update.wage.response.dto';

describe('UpdateWageResponseDto', () => {
  it('should be defined', () => {
    expect(new UpdateWageResponseDto()).toBeDefined();
  });
});
