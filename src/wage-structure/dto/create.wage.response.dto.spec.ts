import { CreateWageResponseDto } from './create.wage.response.dto';

describe('CreateWageResponseDto', () => {
  it('should be defined', () => {
    expect(new CreateWageResponseDto()).toBeDefined();
  });
});
