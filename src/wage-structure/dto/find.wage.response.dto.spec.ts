import { FindWageResponseDto } from '../find.wage.response.dto';

describe('FindWageResponseDto', () => {
  it('should be defined', () => {
    expect(new FindWageResponseDto()).toBeDefined();
  });
});
