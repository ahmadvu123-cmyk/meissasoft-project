import { CreateAttendanceResponseDto } from '../create.attendance.response.dto';

describe('CreateAttendanceResponseDto', () => {
  it('should be defined', () => {
    expect(new CreateAttendanceResponseDto()).toBeDefined();
  });
});
