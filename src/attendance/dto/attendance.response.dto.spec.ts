import { AttendanceResponseDto } from './attendance.response.dto';

describe('AttendanceResponseDto', () => {
  it('should be defined', () => {
    expect(new AttendanceResponseDto()).toBeDefined();
  });
});
