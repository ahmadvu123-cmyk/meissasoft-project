import { DeleteAttendanceResponseDto } from './delete.attendance.response.dto';

describe('DeleteAttendanceResponseDto', () => {
  it('should be defined', () => {
    expect(new DeleteAttendanceResponseDto()).toBeDefined();
  });
});
