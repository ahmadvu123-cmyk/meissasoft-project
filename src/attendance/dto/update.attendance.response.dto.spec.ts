import { UpdateAttendanceResponseDto } from './update.attendance.response.dto';

describe('UpdateAttendanceResponseDto', () => {
  it('should be defined', () => {
    expect(new UpdateAttendanceResponseDto()).toBeDefined();
  });
});
