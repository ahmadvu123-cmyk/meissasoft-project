import { DeletePaymentResponseDto } from '../delete.payment.response.dto';

describe('DeletePaymentResponseDto', () => {
  it('should be defined', () => {
    expect(new DeletePaymentResponseDto()).toBeDefined();
  });
});
