import { CreateChatbotResponseDto } from './create.chatbot.response.dto';

describe('CreateChatbotResponseDto', () => {
  it('should be defined', () => {
    expect(new CreateChatbotResponseDto()).toBeDefined();
  });
});
