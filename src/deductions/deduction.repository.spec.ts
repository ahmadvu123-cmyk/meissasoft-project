import { DeductionRepository } from './deduction.repository';

describe('DeductionRepository', () => {
  it('should be defined', () => {
    expect(new DeductionRepository()).toBeDefined();
  });
});
