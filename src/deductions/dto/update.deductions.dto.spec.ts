import { UpdateDeductionsDto } from './update.deductions.dto';

describe('UpdateDeductionsDto', () => {
  it('should be defined', () => {
    expect(new UpdateDeductionsDto()).toBeDefined();
  });
});
