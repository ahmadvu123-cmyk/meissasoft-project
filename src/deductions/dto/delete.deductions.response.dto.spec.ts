import { DeleteDeductionsResponseDto } from './delete.deductions.response.dto';

describe('DeleteDeductionsResponseDto', () => {
  it('should be defined', () => {
    expect(new DeleteDeductionsResponseDto()).toBeDefined();
  });
});
