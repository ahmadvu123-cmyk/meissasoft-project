import { FindPayrollResponseDto } from '../find.payroll.response.dto';

describe('FindPayrollResponseDto', () => {
  it('should be defined', () => {
    expect(new FindPayrollResponseDto()).toBeDefined();
  });
});
