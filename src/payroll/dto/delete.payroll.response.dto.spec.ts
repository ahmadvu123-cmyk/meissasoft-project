import { DeletePayrollResponseDto } from '../delete.payroll.response.dto';

describe('DeletePayrollResponseDto', () => {
  it('should be defined', () => {
    expect(new DeletePayrollResponseDto()).toBeDefined();
  });
});
