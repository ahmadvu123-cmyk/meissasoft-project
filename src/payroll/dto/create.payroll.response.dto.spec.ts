import { CreatePayrollResponseDto } from './create.payroll.response.dto';

describe('CreatePayrollResponseDto', () => {
  it('should be defined', () => {
    expect(new CreatePayrollResponseDto()).toBeDefined();
  });
});
