import { UpdatePayrollResponseDto } from '../update.payroll.response.dto';

describe('UpdatePayrollResponseDto', () => {
  it('should be defined', () => {
    expect(new UpdatePayrollResponseDto()).toBeDefined();
  });
});
